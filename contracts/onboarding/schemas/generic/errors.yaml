components:
  responses:
    BadRequest:
      description: BadRequest Error description (400)
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
    Unauthenticated:
      description: Unauthenticated Error description (401)
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
    InternalServerError:
      description: InternalServerError Error description (500)
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
  schemas:
    # Schema for error response body
    Error:
      type: object
      title: "Error message"
      properties:
        name:
          type: string
          description: "Human readable, unique name of the error."
        message:
          type: string
          description: "Message describing the error."
        error_id:
          type: string
          description: "Error id to easy search in logs."
      required:
        - name
        - message
        - error_id