type Account {
  id: ID!
  name: String!
  balance: Float!
}

type Query {
  accounts: [Account]
  users: [User]
}

type User {
  id: ID
  name: String
  accounts: [Account!]
}

type Mutation {
  login(id: ID!): User
}

schema {
  query: Query
  mutation: Mutation
}